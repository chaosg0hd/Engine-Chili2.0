#pragma once

namespace pong
{
    constexpr float kBallSpeed    = 4.5f;
    constexpr float kBallHalfSize = 0.1f;   // scale 0.2 → half 0.1
    constexpr float kPaddleHalfX  = 0.075f; // scale.x 0.15 → half 0.075
    constexpr float kPaddleHalfZ  = 0.4f;   // scale.z 0.8  → half 0.4
    constexpr float kPaddleX      = 3.7f;
    constexpr float kWallHalfZ    = 2.5f;
    constexpr float kAiSpeed      = 3.5f;
}
