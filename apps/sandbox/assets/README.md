# Sandbox Assets

This folder is the sandbox-local asset root.

Prototype JSON files live under `prototypes/` for now. ResourceModule can load them with `ResourceKind::PrototypeJson`; a later prototype loader can parse the loaded JSON text into registered engine prototypes.
